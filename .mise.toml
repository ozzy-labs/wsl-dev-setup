[tools]
# Runtime & package managers (commitlint に必要)
node = "24"
pnpm = "10"

# Linters & formatters
shellcheck = "0.11"
shfmt = "3"
"npm:markdownlint-cli2" = "0.21"
"pipx:yamllint" = "1"
yamlfmt = "0.21"
actionlint = "1"
gitleaks = "8"
"pipx:mdformat" = "0.7"

# Git hooks
lefthook = "2"
