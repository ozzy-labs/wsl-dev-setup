{
	"$schema": "https://json.schemastore.org/claude-code-settings.json",
	"theme": "auto",
	"permissions": {
		"autoAccept": [
			"Read",
			"Bash(git status)",
			"Bash(ls -F)",
			"Bash(pnpm run build)",
			"Bash(pnpm run typecheck)"
		]
	}
}
